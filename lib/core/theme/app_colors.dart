import 'package:flutter/cupertino.dart';

sealed class AppColors {
  static const midnight = Color(0xFF0B0F1A);
  static const midnightRaised = Color(0xFF0F1626);
  static const stroke = Color(0xFF1B2942);

  static const accent = Color(0xFF3ABEFF);
  static const accentMuted = Color(0x803ABEFF);

  static const textPrimary = Color(0xFFEAF1FF);
  static const textSecondary = Color(0xFF96A3BA);

  static const card = Color(0xCC0F1626);
  static const cardSolid = Color(0xFF0F1626);

  static const separator = Color(0x331B2942);
}

